<br>
<div class = "container">
  <div class = "row">
    <h3>商品詳細ページ</h3>
  </div>
  <div class="row">
	  <div class="col-xs-3">
      <%= attachment_image_tag @item, :item_image, size: "200x200", fallback: "download.jpg"%><br>
    </div>
    <div class="col-xs-9">
      <table>
        <tbody>
          <tr>
            <th>商品名</th>
            <td><%= @item.item_name %></td>
          </tr>
          <tr>
            <th>商品説明</th>
            <td><%= @item.item_profile %></td>
          </tr>
          <tr>
            <th>ジャンル</th>
            <td><%= @item.genre.genre_name %></td>
          </tr>
          <tr>
            <th>税込価格(税抜き)</th>
            <td><%= (@item.price * 1.1r).to_i %> (<%= @item.price %>)</td>
          </tr>
          <tr>
            <th>販売ステータス</th>
              <td><% if @item.sale_status %>
                    販売中
                  <% else %>
                    販売停止中
                  <% end %>
              </td>
          </tr>
        </tbody>
      </table>

      <br>

      <%= link_to "編集", edit_admin_item_path(@item.id), class: 'btn btn-success' %>
    </div>
  </div>
</div>