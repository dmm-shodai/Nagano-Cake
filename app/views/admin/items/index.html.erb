商品一覧<br>
　<table class = 'table'>
   <thead>
   	<tr>
   	 <th>商品ID</th>
   	 <th>商品名</th>
   	 <th>税抜き価格</th>
   	 <th>ジャンル</th>
   	 <th>ステータス</th>
   	</tr>
   </thead>
   <tbody>
    <% @items.each do |item| %>
    <tr>
     <td><%= item.id %></td>
     <td><%= link_to item.item_name, admin_item_path(item.id) %></td>
     <td><%= item.price %></td>
    <% if item.genre != nil %>
     <td><%= item.genre.genre_name %></td>
    <% end %>
    <% if item.sale_status %>
      <td>販売中</td>
    <% else %>
      <td>販売停止中</td>
    <% end %></tr>
   <% end %>
  </tbody>
 </table>
